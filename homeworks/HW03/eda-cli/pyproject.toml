[project]
name = "eda-cli"
version = "0.1.0"
description = "Mini EDA CLI tool"
dependencies = [
    "pandas",
    "matplotlib",
    "click"
]

[project.scripts]
eda-cli = "eda_cli.cli:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
